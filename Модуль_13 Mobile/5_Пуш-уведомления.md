Пуш-уведомления — один из самых сложных и важных аспектов мобильного тестирования.  
Они зависят от ОС, производителя, сетевых условий, фоновой работы приложений и настройки пользователя.

Поэтому важно понимать **как они работают**, **какие бывают**, **как тестировать** и **почему они так часто “не приходят”**.

---
# 🧩 1. Что такое пуш-уведомления

**Пуш-уведомление** — это сообщение, которое отправляется приложению _через сервер ОС_, даже когда приложение закрыто.

Это не SMS, не email и не всплывающее окно — это системное уведомление, доставляемое через специальные сервисы:

- **FCM (Firebase Cloud Messaging)** — для Android
- **APNs (Apple Push Notification Service)** — для iOS

Приложение не может “само себе отправить пуш”.  
Всегда участвуют **сервер → сервис ОС → устройство → приложение**.

---
# 🌐 2. Как устроена цепочка доставки (простая схема)

`Бэкенд → FCM/APNs → устройство → системная шторка → приложение`

Если где-то в цепочке проблема — пуш не придёт.

---
# 🧭 3. Виды пуш-уведомлений

## 📌 Стандартные уведомления
Обычные текстовые/иконные уведомления в статус-баре.

## 📌 Уведомления с действиями
Кнопки: “Ответить”, “Удалить”, “Открыть чат”, “Развернуть”.

QA проверяет **каждый сценарий поведения**.

## 📌 Тихие уведомления (Silent Push)
Пользователь их не видит.  
Используются для:

- обновления данных
- синхронизации
- уведомлений внутри приложения
- аналитики

❗ Самые «сложные» для тестирования.

## 📌 Push-to-Open (deep links)
Уведомление, которое открывает конкретный экран:

- товар
- чат
- заказ
- акцию
- профиль

QA должен проверить переходы, даже если приложение было:

- закрыто
- в фоне
- выгружено системой

---
# 📱 4. Состояния приложения и поведение пушей

Очень важно понимать, как пуш ведёт себя в разных состояниях.

|Состояние приложения|Что происходит|
|---|---|
|**Открыто**|Приходит событие → UI обновляется (без уведомления)|
|**В фоне**|Пуш приходит → попадает в шторку|
|**Закрыто (но не выгружено)**|Работает как обычно|
|**Выгружено системой**|На Android может не прийти; на iOS приходит всегда|
|**Режим энергосбережения**|Может задерживаться, часто не приходит|
|**Нет интернета / плохая сеть**|Доставка откладывается|

---
# 🧨 5. Почему пуш может не приходить (реальные причины)

Проблемы могут быть:

### 📌 На стороне устройства:
- отключены уведомления
- ограничение фоновой активности
- режим энергосбережения
- нет интернета
- включён режим Doze (включён режим Android)
- проблемы с производителем (MIUI, EMUI и др.)

### 📌 На стороне сервера:
- пуш не отправлен
- ограничение частоты (как часто сервер может отправлять пуши одному устройству)
- неверный токен устройства
- токен устарел

### 📌 На стороне ОС:
- **FCM (Firebase Cloud Messaging**) — сервис Google для доставки пушей.  
Если много сообщений одновременно или есть нагрузка на сервер, FCM может **задерживать или отклонять пуши**.
- **APNs (Apple Push Notification Service)** — сервис Apple.  
Чтобы пуши работали, приложение должно иметь **действительный сертификат**.
Если сертификат истёк, отозван или неправильно настроен, APNs **не доставит уведомления на устройство**.
- **push задержан из-за слабой сети** Даже если пуш отправлен сервером, ОС может **отложить его доставку**, если:
	- сигнал Wi-Fi слабый
	- мобильная сеть нестабильна
	- устройство переключается между сетями (LTE ↔ 3G ↔ Wi-Fi)

---
# 🧪 6. Что должен проверять QA (чек-лист)

## 📍 Базовые проверки
- приложение получает пуш
- приложение корректно открывается (deep link)
- пуш исчезает после открытия
- пуш срабатывает при разных состояниях приложения

## 📍 Проверка UI уведомления
- текст
- иконка
- кнопки действий
- изображения (rich push)
- локализация

## 📍 Проверка поведения
- пуш приходит в фоне
- пуш приходит при заблокированном экране
- push-to-open открывает нужный экран
- уведомление остаётся в шторке или пропадает — по требованиям

## 📍 Тестирование негативных сценариев
- отключены разрешения
- отключена фоновая работа
- нулевая батарея / режим энергосбережения
- плохая сеть / airplane mode
- много пушей подряд (rate limit)

---
# 🧲 7. Push-токен: важная часть тестирования

Каждое устройство имеет **уникальный токен** для получения пушей.

QA должен уметь проверять:

- токен генерируется при первом запуске
- токен обновляется
- токен передаётся на сервер
- токен меняется после reinstall
- токен меняется после logout (если заложено логикой)

---
# 🔁 8. Повторная доставка и очереди

Push может быть:

- доставлен мгновенно
- доставлен с задержкой
- недоставлен (и не будет доставлен)
- доставлен после долгой паузы (когда сеть появится)

Сервер может иметь политику:

| Политика            | Что делает                                                 | Особенности                                                                                                                         | Пример из практики QA                                                                                                                                           |
| ------------------- | ---------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **Retry**           | Повторная отправка пуша, если он не доставлен              | Количество попыток и время жизни пуша ограничено. Используется при временных сбоях сети или устройства.                             | Пользователь был оффлайн 10 минут → пуш с обновлением заказа повторно доставлен после выхода в сеть. QA проверяет корректное отображение уведомления.           |
| **Drop**            | Пуш отбрасывается, если не доставлен                       | Используется для устаревших или некритичных уведомлений. Уведомление не будет показано.                                             | Пуш о спецпредложении, срок действия которого истёк. Пользователь был оффлайн 2 часа → уведомление не пришло. QA проверяет, что приложение работает без ошибок. |
| **Priority High**   | Пуш доставляется сразу, даже при энергосбережении или Doze | Используется для критичных уведомлений: платежи, ошибки, срочные сообщения.                                                         | Пуш «Ваш платёж не прошёл» доставляется мгновенно. QA проверяет, что уведомление появляется вне зависимости от состояния сети.                                  |
| **Priority Normal** | Пуш может быть отложен системой                            | Может задерживаться при слабой сети или режиме энергосбережения. Используется для уведомлений с низким приоритетом: акции, новости. | Пуш «Новая акция на сайте» доставлен с задержкой после восстановления сети. QA проверяет корректность отображения после задержки.                               |

QA должен знать эти механики, чтобы понимать, где искать баг.

---

# 🧩 9. Мини-кейс: реальная проблема

> Приложение: e-commerce  
> Пуш не приходил пользователям с Xiaomi и Huawei.
> Причина: прошивки убивали приложение и запрещали работу в фоне.
> Решение:  
> добавить инструкцию пользователю + слать пуш как **priority high**.
