## 📌 Что такое WebView

**WebView** — это встроенный браузер внутри мобильного приложения.  
Он позволяет отображать веб-страницы **как часть интерфейса приложения**, без открытия внешнего браузера.

Используется в **гибридных приложениях** (React Native, Cordova, Ionic) и во многих нативных (например, для отображения динамического контента, оплаты, профилей, лендингов).

> 💡 Проще говоря: **гибридное приложение — это смесь нативного контейнера и WebView, в который загружается веб-контент.**

---

## 🔍 Зачем QA работать с WebView через DevTools

WebView — это фактически **мини-браузер**, а значит:

- там есть **DOM**,
- **JS-логика**,
- **сетевые запросы**,
- **консольные ошибки**,
- **кэш и storage**.

Chrome DevTools позволяет «заглянуть внутрь» WebView так же, как в обычный сайт.

### QA использует DevTools для:

- анализа отображения элементов (Elements);
- поиска ошибок JS (Console);
- проверки API (Network);
- проверки кэша, cookie, localStorage (Application);
- просмотра логики маршрутизации, редиректов;
- выявления проблем WebView (необновление контента, медленная загрузка).

---

## ⚙️ Как подключиться к WebView через Chrome DevTools (Android)

1. Включи USB-отладку на устройстве
`Настройки → Для разработчиков → Отладка по USB`
2. Подключи устройство к компьютеру через USB
3. Открой на ПК:`chrome://inspect/#devices`
4. Запусти гибридное приложение и перейди на экран с WebView
Важно: WebView должен быть **активен**.  
Если ты на нативном экране — он не появится.
	Допустим, у тебя приложение с такими экранами:
	- экран авторизации — **нативный**
	- главная страница — **нативная**
	- каталог товаров — **WebView**
	- корзина — **WebView**
	- настройки — **нативный**
	Если ты открыл **главную страницу (нативную)** →  
	в `chrome://inspect` НЕ появится ничего.
	
	Если ты открыл **каталог товаров (WebView)** →  
	DevTools покажет:
	
	👉 `WebView in com.appname`  
	и появится кнопка [inspect]
5. В Chrome появится:
**«WebView in com.appname» → [inspect]**
Нажимаешь **inspect**, открывается знакомый Chrome DevTools.

---

## 🍎 Как открыть WebView на iOS (Safari Web Inspector)

1. На iPhone включи:
    `Настройки → Safari → Дополнительно → Web Inspector`
2. Подключи iPhone к Mac.
3. На Mac в Safari открой:  
    **Develop → [Название устройства] → [WebView внутри приложения]**

Это — iOS-аналог DevTools.

![[Сравнение WebView и нативных экранов 1.png]]

---

## 🧩 Что проверяет QA в DevTools

| 🧪 Область         | 🔍 Что смотрим                                                     |
| ------------------ | ------------------------------------------------------------------ |
| **Верстка и UI**   | DOM, стили, адаптивность, web-шрифты                               |
| **JS-ошибки**      | Console — ошибки, логирование, предупреждения                      |
| **API и сеть**     | Network — запросы, ответы, тело, статус-коды                       |
| **Кэш приложения** | LocalStorage, sessionStorage, cookies, cache                       |
| **Перфоманс**      | Performance — рендеринг, FPS, время загрузки                       |
| **WebView баги**   | Обновление контента, ошибки загрузки, зависания, повторные запросы |

---

## 🧠 Типичные баги WebView, которые находят QA

| 🐞 Баг                 | 💬 Причина                       | 🔧 Проверка                 |
| ---------------------- | -------------------------------- | --------------------------- |
| Контент не обновляется | WebView кэширует старую страницу | Disable cache / Hard reload |
| API не отвечает        | Неверный токен, ошибки           | Network → заголовки         |
| Разъехалась верстка    | Неправильные CSS-медиа-запросы   | Elements → размеры          |
| Белый экран            | JS-ошибка блокирует рендер       | Console → красная ошибка    |
| Многократные запросы   | Двойная инициализация WebView    | Network → повторные вызовы  |

---

## ⚠️ Важный нюанс: почему WebView может не появиться в DevTools

Причины:

1. **USB debugging включён, но телефон не доверяет ПК**  
    → Нажать «Разрешить».
    
2. **На экране нет WebView**  
    → Открыть страницу, где есть веб-контент.
    
3. **DevTools-доступ отключён в коде приложения**  
    Разработчик должен включить:
    
    `WebView.setWebContentsDebuggingEnabled(true);`
    
4. **Используется старый WebView (Android < 5.0)**  
    → Устройство слишком старое.

---

## 📘 Выводы подмодуля

После изучения блока студент умеет:

- Понимать, что такое WebView и как он работает внутри приложения;
- Подключать устройства к Chrome DevTools и Safari Inspector;
- Анализировать DOM, сетевые запросы, консоль и кэш WebView;
- Находить типичные WebView-баги;
- Отличать проблемы фронта от проблем контейнера.