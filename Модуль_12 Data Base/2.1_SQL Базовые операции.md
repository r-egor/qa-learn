## 🎯 Разделение на подмодули

| Подмодуль                          | Содержание                                                           |
| ---------------------------------- | -------------------------------------------------------------------- |
| 🧱 [[2.2_CREATE INSERT]]           | Создание таблиц и добавление данных (`CREATE`, `INSERT`)             |
| 🔍 [[2.3_SELECT WHERE]]            | Извлечение данных и фильтрация (`SELECT`, `WHERE`)                   |
| ✏️ [[2.4_UPDATE DELETE]]           | Изменение и удаление данных (`UPDATE`, `DELETE`)                     |
| 📊 [[2.5_ORDER_BY LIMIT]]          | Сортировка и ограничение выборки (`ORDER BY`, `LIMIT`)               |
| 🧮 [[2.6_GROUP BY HAVING]]         | Объединяет строки, которые имеют одинаковые значения (`GROUP BY`)    |
| 🤝 [[2.7_JOIN объединение таблиц]] | Соединять данные из нескольких таблиц при помощи операторов (`JOIN`) |

---

## 📌 Пример базы данных, с которой будем работать

``` sql
CREATE TABLE IF NOT EXISTS customers (
    userid VARCHAR(13) CHARACTER SET utf8,
    firstname VARCHAR(9) CHARACTER SET utf8,
    lastname VARCHAR(8) CHARACTER SET utf8,
    password VARCHAR(23) CHARACTER SET utf8,
    credit_card BIGINT,
    country VARCHAR(9) CHARACTER SET utf8,
    email VARCHAR(33) CHARACTER SET utf8,
    price INT
);
```

💾 Таблица содержит клиентов с различными полями — имена, страны, e-mail и суммы покупок.  
Это отличная база для практики QA, потому что можно тестировать:

- корректность данных,
- фильтрацию по условиям,
- обновление и удаление записей,
- сортировки и отчёты.