## 💡 Что такое реляционная модель

**Реляционная модель данных** — это способ организации информации в виде **таблиц**, где каждая таблица (relation) хранит данные об определённых объектах, а между таблицами существуют **связи** (relations).

📘 Термин “реляционная” происходит от слова _relation_ — **отношение, связь**.

---

## 🧱 Основные элементы реляционной модели

|🧩 Элемент|📘 Описание|💬 Пример|
|---|---|---|
|📄 **Таблица (table)**|Хранит данные об определённой сущности|`users`, `orders`, `products`|
|🔢 **Строка (row, record)**|Одна запись, конкретный объект|`ID=1, Name='Anna'`|
|📊 **Столбец (column, field)**|Атрибут или свойство сущности|`name`, `email`, `price`|
|🔑 **Первичный ключ (Primary Key)**|Уникальный идентификатор строки|`user_id`|
|🔗 **Внешний ключ (Foreign Key)**|Связь с другой таблицей|`user_id` в таблице `orders`|
|🧠 **Отношение (relation)**|Логическая связь между таблицами|“Один пользователь — много заказов”|

---

## 📘 Пример структуры

**Таблица `users`**

|id|name|email|
|---|---|---|
|1|Anna|anna@mail.com|
|2|Max|max@mail.com|

**Таблица `orders`**

|id|user_id|product|price|
|---|---|---|---|
|1|1|Laptop|1200|
|2|1|Mouse|25|
|3|2|Phone|800|

🧩 Здесь:

- `id` — **первичный ключ** (уникальная запись в таблице).
- `user_id` — **внешний ключ**, который связывает заказ с пользователем.
- Один пользователь (`users.id = 1`) может иметь несколько заказов — это **связь “один ко многим” (1:N)**. 

---

## ⚙️ Как это выглядит в SQL

`SELECT u.name, o.product, o.price FROM users u JOIN orders o ON u.id = o.user_id;`

📊 **Результат:**

|name|product|price|
|---|---|---|
|Anna|Laptop|1200|
|Anna|Mouse|25|
|Max|Phone|800|

---

## 🧭 Зачем QA нужно понимать реляционную модель

QA-инженеру важно разбираться в реляционных связях, чтобы:

- 🔍 Проверять, **правильно ли сохраняются данные** в связанных таблицах.
- 🧾 Понимать, **откуда берутся данные в UI или отчётах**.
- 🧠 Тестировать корректность **JOIN-запросов** и связей (особенно при интеграционном тестировании).
- ⚙️ Быстро находить и удалять **тестовые данные** без нарушения связей.

---

## 🧩 Типы ключей

| 🔑 Тип ключа         | 📘 Назначение                      | 💬 Пример             |
| -------------------- | ---------------------------------- | --------------------- |
| 🆔 **Primary Key**   | Уникальный идентификатор записи    | `id`                  |
| 🔗 **Foreign Key**   | Ссылается на ключ в другой таблице | `user_id` в `orders`  |
| 🧮 **Composite Key** | Состоит из нескольких полей        | `(user_id, order_id)` |
| 🗝️ **Unique Key**   | Гарантирует уникальность значения  | `email` пользователя  |

---

## 🧠 Аналогия для понимания

> Представь базу данных как Excel-файл:
> 
> - Каждый лист — это таблица (`users`, `orders`);
> - Каждая строка — запись;
> - Колонки — атрибуты;
> - Формула со ссылкой на другой лист — это **внешний ключ (Foreign Key)**.