# Виды и принципы организации данных

## 💡 Что такое модель базы данных

**Модель базы данных** — это способ **организации и представления данных** внутри базы. Она определяет, **как данные связаны между собой**, как они хранятся и как к ним обращаться. Проще говоря, модель — это **“архитектура” базы данных**.

---

## 🧩 Зачем QA нужно понимать модели БД

Для тестировщика знание моделей помогает:

- 🔍 Понимать, **где и как хранятся данные** приложения;
- 🧾 Быстро находить нужную информацию в разных таблицах;
- 🔗 Проверять **связи между сущностями** (пользователь — заказ, заказ — товар и т.д.);
- 🧠 Разбираться в логике работы API и backend-тестов;
- 📊 Понимать, как данные связаны при SQL-запросах (JOIN).

---

## 🗂️ Основные типы моделей баз данных

| 🧱 Модель            | 📘 Краткое описание                                                                    | 💬 Пример / Аналогия                              |
| -------------------- | -------------------------------------------------------------------------------------- | ------------------------------------------------- |
| 🌳 **Иерархическая** | Данные организованы в виде **дерева** — каждый элемент имеет родителя и потомков.      | Структура папок в файловой системе.               |
| 🌐 **Сетевая**       | Каждый элемент может иметь **несколько связей** (не одно дерево, а сеть).              | Социальная сеть: один человек связан с многими.   |
| 📋 **Реляционная**   | Данные хранятся в **таблицах** (строки, столбцы), которые можно связывать между собой. | Таблицы Excel с общими колонками.                 |
| 📦 **Объектная**     | Данные представлены как **объекты** с атрибутами и методами (похоже на OOP).           | Пример: класс `User` с полями и методами.         |
| 📜 **Документная**   | Хранит **документы JSON или XML** без строгой структуры.                               | MongoDB — хранит “гибкие” данные в виде объектов. |
| 🗝️ **Key-Value**    | Простая структура: ключ → значение. Очень быстрая.                                     | Redis, кэш браузера.                              |

---

## 📊 Наиболее используемая модель — реляционная

На практике большинство приложений (и, соответственно, тестов) работают с **реляционными базами данных**  
— это **MySQL, PostgreSQL, SQLite**, Oracle и другие.

🧩 В них данные хранятся в виде **таблиц**, где:

| Термин                  | Описание                          | Пример                            |
| ----------------------- | --------------------------------- | --------------------------------- |
| 📄 **Таблица**          | Набор данных по определённой теме | `users`, `orders`, `products`     |
| 🔢 **Строка (row)**     | Одна запись                       | Пользователь: ID=1, Имя=“Anna”    |
| 🧱 **Столбец (column)** | Поле или атрибут                  | `id`, `name`, `email`             |
| 🔑 **Ключ (key)**       | Уникальный идентификатор записи   | `user_id`, `order_id`             |
| 🔗 **Связь (relation)** | Отношение между таблицами         | Один пользователь — много заказов |

---

## 🧠 Аналогия для QA

> Представь Excel-файл:
> - каждый лист — это **таблица**;
> - каждая строка — это **запись**;
> - колонки — это **поля**;
> - между листами можно делать **ссылки** — это и есть **связи** в БД.

---

## 🧪 Пример для понимания

**Таблица `users`**

|id|name|email|
|---|---|---|
|1|Anna|anna@mail.com|
|2|Max|max@mail.com|

**Таблица `orders`**

|id|user_id|product|price|
|---|---|---|---|
|1|1|Laptop|1200|
|2|2|Phone|800|

➡️ Здесь `user_id` связывает таблицы:  
**один пользователь** может иметь **много заказов**.