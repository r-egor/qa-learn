## 🎯 Цель
Научиться:
- получать нужные данные с помощью `SELECT`
- фильтровать результаты с помощью `WHERE`
- использовать условия (`AND`, `OR`, `IN`, `LIKE`, `BETWEEN`)
- применять QA-мышление при проверке корректности данных

## 🧩 1. Основной оператор — `SELECT`

Команда `SELECT` извлекает данные из таблицы.  
Самый простой пример:
```sql
SELECT * FROM customers;
```
💡 `*` — значит “выбрать все столбцы”.
💡 `SELECT DISTINCT` используется для возврата только различных значений.

Пример: выбор конкретных столбцов
```sql
SELECT firstname, lastname, email FROM customers;
```
💡 Выведет только имена, фамилии и e-mail всех клиентов.

## 🧩 2. Условие фильтрации — `WHERE`

С помощью `WHERE` можно выбрать только нужные строки.
```sql
SELECT firstname, lastname, country, price
FROM customers
WHERE country = 'Germany';
```
💡 Выведет всех клиентов из Германии 🇩🇪

Комбинирование условий
Пример: несколько условий с `AND`
```sql
SELECT firstname, lastname, country, price
FROM customers
WHERE country = 'Germany' AND price > 1000;
```
💡 Покажет только 🇩🇪 немецких клиентов с 💳 покупками больше 1000 💵.

Пример: `OR`
``` sql
SELECT firstname, country, price
FROM customers
WHERE country = 'Russia' OR country = 'France';
```
💡 Покажет всех клиентов из России 🇷🇺 и Франции 🇫🇷.

`IN / NOT IN` — несколько значений в одном условии
```sql
SELECT firstname, country
FROM customers
WHERE country IN ('Russia', 'Germany', 'USA');
```

`LIKE` — поиск по шаблону
```sql
SELECT firstname, email
FROM customers
WHERE email LIKE '%gmail.com';
```
📧 Выведет всех пользователей с почтой на Gmail.

| 🧩 Пример запроса                 | 📝 Описание                                                                        |
| --------------------------------- | ---------------------------------------------------------------------------------- |
| `WHERE CustomerName LIKE 'a%'`    | Находит все значения, **которые начинаются с** `"a"`                               |
| `WHERE CustomerName LIKE '%a'`    | Находит все значения, **которые заканчиваются на** `"a"`                           |
| `WHERE CustomerName LIKE '%or%'`  | Находит все значения, **в которых встречается** `"or"` **в любом месте**           |
| `WHERE CustomerName LIKE '_r%'`   | Находит все значения, **у которых вторая буква** — `"r"`                           |
| `WHERE CustomerName LIKE 'a_%_%'` | Находит все значения, **начинающиеся с** `"a"` **и длиной не менее трёх символов** |
| `WHERE ContactName LIKE 'a%o'`    | Находит все значения, **которые начинаются с** `"a"` **и заканчиваются на** `"o"`  |
`BETWEEN` — диапазон значений
``` sql
SELECT firstname, lastname, price
FROM customers
WHERE price BETWEEN 1000 AND 5000;
```
💰 Выведет всех клиентов, у кого сумма покупок от 1000 до 5000 включительно.

`IS NULL` и `IS NOT NULL`
Если есть пропущенные значения (например, в тестовых данных):
```sql
SELECT * FROM customers WHERE email IS NULL;
```

## ✅ Краткое резюме

|Команда|Назначение|Пример|
|---|---|---|
|`SELECT`|Извлечь данные|`SELECT * FROM customers;`|
|`WHERE`|Фильтрация строк|`WHERE country = 'France'`|
|`AND` / `OR`|Комбинирование условий|`WHERE price > 1000 AND country='USA'`|
|`IN`|Несколько значений|`WHERE country IN ('USA','Germany')`|
|`LIKE`|Поиск по шаблону|`WHERE email LIKE '%gmail%'`|
|`BETWEEN`|Диапазон значений|`WHERE price BETWEEN 1000 AND 5000`
