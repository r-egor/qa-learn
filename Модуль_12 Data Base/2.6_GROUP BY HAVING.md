## 🎯 Цель

Научиться группировать данные по определённым признакам,  
анализировать агрегаты по группам и фильтровать результаты с помощью `HAVING`.

---

## 📦 Что делает `GROUP BY`

Команда `GROUP BY` объединяет строки, которые имеют **одинаковые значения** в выбранных столбцах, и позволяет применять к ним агрегатные функции (`COUNT`, `AVG`, `SUM`, `MIN`, `MAX`).

### 🔹 Синтаксис
```sql
SELECT column, aggregate_function(column) FROM table GROUP BY column;
```

---

📘 Примеры
1. Подсчитать количество клиентов в каждой стране
```sql
SELECT country, COUNT(*) AS total_customers FROM customers GROUP BY country;
```

📊 Результат:

|country|total_customers|
|---|---|
|Russia|6|
|Germany|5|
|USA|3|
|France|6|
|Australia|5|
|Egypt|2|
|Mexico|4|

---

2. Средняя цена (`price`) по каждой стране
```sql
SELECT country, AVG(price) AS avg_price FROM customers GROUP BY country ORDER BY avg_price DESC;
```
📈 Покажет, в каких странах пользователи тратят больше.

---

3. Общая сумма (`SUM`) по странам, где сумма больше 10 000
```sql
SELECT country, SUM(price) AS total_sum FROM customers GROUP BY country HAVING total_sum > 10000;
```
💡 Здесь `HAVING` используется **вместо** `WHERE`,  
потому что фильтрация идёт **по результату агрегатной функции**.

---

## ⚖️ Разница между `WHERE` и `HAVING`

|Ключевое слово|Когда применяется|Пример|
|---|---|---|
|`WHERE`|До группировки (фильтрует строки)|`WHERE country = 'Germany'`|
|`HAVING`|После группировки (фильтрует группы)|`HAVING AVG(price) > 5000`|

---

🔍 Примеры с `HAVING`
 1. Показать только те страны, где более 3 клиентов
```sql
SELECT country, COUNT(*) AS count_users FROM customers GROUP BY country HAVING COUNT(*) > 3;
```

 2. Найти страны, где средняя цена выше 10000
```sql
SELECT country, AVG(price) AS avg_price FROM customers GROUP BY country HAVING AVG(price) > 10000;
```

3. Показать страны с максимальной покупкой свыше 50 000
```sql
SELECT country, MAX(price) AS max_purchase FROM customers GROUP BY country HAVING MAX(price) > 50000;
```

