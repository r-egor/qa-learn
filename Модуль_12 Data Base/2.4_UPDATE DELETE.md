## 🎯 Цель

Научиться **изменять** и **удалять** данные в таблице.  
Это важный навык для QA, особенно при:

- подготовке тестовых данных,
- сбросе состояния после тестов,
- анализе и правке некорректных записей.
## ✏️ Команда `UPDATE` — обновление данных
Позволяет **изменить существующие значения** в таблице.
🔹 Синтаксис
```sql
UPDATE table_name
SET column1 = value1, column2 = value2, ...
WHERE condition;
```
⚠️ **Важно:** без `WHERE` обновятся все строки таблицы!

📘 Примеры
1. Изменить страну пользователя по логину
```sql
UPDATE customers
SET country = 'Canada'
WHERE userid = 'alex2020';
```

2. Изменить (`price`) для всех клиентов из России
```sql
UPDATE customers
SET price = price + 100
WHERE country = 'Russia';
```

3. Обновить e-mail и пароль конкретного пользователя
```sql
UPDATE customers
SET email = 'alex.new@mail.com', password = 'NewPass123'
WHERE userid = 'alex2020';
```

4. Установить скидку для всех, у кого `price` больше 10000
```sql
UPDATE customers
SET price = price * 0.9
WHERE price > 10000;
```

## 🧹 Команда `DELETE` — удаление данных
Удаляет строки из таблицы.  
Как и с `UPDATE`, **обязательно указывай условие `WHERE`!**
🔹 Синтаксис
```sql
DELETE FROM table_name
WHERE condition;
```

📘 Примеры
1. Удалить конкретного пользователя
```sql
DELETE FROM customers
WHERE userid = 'unknow_man';
```

2. Удалить всех тестовых пользователей (по шаблону логина)
```sql
DELETE FROM customers
WHERE userid LIKE 'test%';
```

3. Удалить всех пользователей из Египта
```sql
DELETE FROM customers
WHERE country = 'Egypt';
```

4. Удалить все строки, где `price = 0`
```sql
DELETE FROM customers
WHERE price = 0;
```

## ⚙️ Совет

- Перед удалением или массовым изменением **всегда** проверь условие через `SELECT`:
```sql
SELECT * FROM customers WHERE country='Egypt';
```
Если результат верный — тогда выполняй `DELETE` или `UPDATE`.
