## 🎯 Цель

Научиться:

- сортировать данные (`ORDER BY`),
- ограничивать количество строк (`LIMIT`),
- использовать агрегатные функции (`COUNT`, `SUM`, `AVG`, `MIN`, `MAX`)  
    для анализа данных и тестирования корректности бизнес-логики.
---
## 🔽 Сортировка данных — `ORDER BY`
Позволяет отсортировать результат по одному или нескольким столбцам.
🔹 Синтаксис
```sql
SELECT columns
FROM table
ORDER BY column [ASC|DESC];
```

| Параметр | Описание                      |
| -------- | ----------------------------- |
| `ASC`    | по возрастанию (по умолчанию) |
| `DESC`   | по убыванию                   |
📘 Примеры
1. Список клиентов по возрастанию цены
```sql
SELECT firstname, lastname, price
FROM customers
ORDER BY price ASC;
```

2. Самые дорогие клиенты
```sql
SELECT userid, price
FROM customers
ORDER BY price DESC;
```

3. Сортировка по нескольким полям
```sql
SELECT firstname, lastname, country
FROM customers
ORDER BY country ASC, lastname DESC;
```

## 🔢 Ограничение выборки — `LIMIT`
Используется для вывода **только части результата**, например, первых N строк.
🔹 Синтаксис
```sql
SELECT columns
FROM table
LIMIT n;
```

📘 Примеры
1. Показать 5 первых клиентов
```sql
SELECT * FROM customers
LIMIT 5;
```

2. Показать топ-3 клиента по цене
```sql
SELECT firstname, price
FROM customers
ORDER BY price DESC
LIMIT 3;
```

3. Пропустить первые 10 строк и взять следующие 5
```sql
SELECT * FROM customers
LIMIT 5 OFFSET 10;
```

## 📊 Агрегатные функции

Используются для анализа и статистики.  
Применяются с числовыми полями и часто сочетаются с `GROUP BY`.

| Функция   | Назначение                    | Пример       |
| --------- | ----------------------------- | ------------ |
| `COUNT()` | Подсчитывает количество строк | `COUNT(*)`   |
| `SUM()`   | Суммирует значения            | `SUM(price)` |
| `AVG()`   | Среднее значение              | `AVG(price)` |
| `MIN()`   | Минимальное значение          | `MIN(price)` |
| `MAX()`   | Максимальное значение         | `MAX(price)` |
 📘 Примеры

1. Посчитать общее количество клиентов
```sql
SELECT COUNT(*) AS total_customers FROM customers;
```

2. Узнать среднюю цену по всем клиентам
```sql
SELECT AVG(price) AS avg_price FROM customers;
```

 3. Найти минимальную и максимальную цену
```sql
SELECT MIN(price) AS min_price, MAX(price) AS max_price FROM customers;
```

4. Сумма всех покупок клиентов из России
```sql
SELECT SUM(price) AS total_russia FROM customers WHERE country = 'Russia';
```

5. Средняя цена по каждой стране
```sql
SELECT country, AVG(price) AS avg_price FROM customers GROUP BY country ORDER BY avg_price DESC;
```
