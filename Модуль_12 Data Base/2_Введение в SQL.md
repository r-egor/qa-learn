## 🧩 Введение в язык структурированных запросов (SQL)

## 💡 Что такое SQL

**SQL (Structured Query Language)** — это язык для работы с базами данных.  
Он позволяет **создавать, изменять, получать и удалять данные** в реляционных СУБД (MySQL, PostgreSQL, SQLite и др).

📊 SQL — это универсальный инструмент, который используют **QA-инженеры, разработчики, аналитики и DevOps**.

---
## 🎯 Почему SQL важен для QA

Для тестировщика знание SQL — один из ключевых навыков:

|🧩 Ситуация|🔍 Что делает QA|🧠 Зачем|
|---|---|---|
|После регистрации нового пользователя|Проверяет запись в таблице `users`|Убедиться, что данные сохранились корректно|
|После оформления заказа|Проверяет таблицу `orders`|Проверить, что сумма, статус и связи с пользователем верные|
|Перед автотестом|Очищает тестовые данные|Подготовить систему к тесту|
|После теста|Удаляет тестовые данные|Избежать конфликтов в последующих проверках|

---

## 🧱 Основные возможности SQL

SQL можно разделить на несколько **групп команд**, каждая решает свою задачу:

| 💬 Категория                           | 📘 Назначение                                        | 🧩 Примеры команд            |
| -------------------------------------- | ---------------------------------------------------- | ---------------------------- |
| **DDL (Data Definition Language)**     | Определение структуры БД (создание таблиц, индексов) | `CREATE`, `ALTER`, `DROP`    |
| **DML (Data Manipulation Language)**   | Работа с данными (добавление, изменение, удаление)   | `INSERT`, `UPDATE`, `DELETE` |
| **DQL (Data Query Language)**          | Получение данных из таблиц                           | `SELECT`                     |
| **DCL (Data Control Language)**        | Управление правами доступа                           | `GRANT`, `REVOKE`            |
| **TCL (Transaction Control Language)** | Управление транзакциями                              | `COMMIT`, `ROLLBACK`         |

---

🧾 Пример простого SQL-запроса

``` sql
SELECT name, email
FROM users
WHERE is_active = 1
ORDER BY created_at DESC;
```

🧠 Что делает этот запрос:
- выбирает имя и email активных пользователей,
- сортирует по дате создания, от новых к старым.
---
## 📚 Советы по SQL для тестировщика

- 🧠 Не пиши запрос “наугад” — проверяй структуру таблицы (`DESCRIBE table_name;`).
- 🧩 Делай **резервные копии** перед `UPDATE` и `DELETE`.
- 🧹 После тестов очищай данные, чтобы не мешать другим тестам.
- 🕵️‍♀️ Изучай **JOIN-запросы** — без них не обойтись в реальных проектах.
- 🚀 Постепенно переходи от простых выборок к аналитическим запросам (`GROUP BY`, `COUNT`, `HAVING`).